# Internal Docker address of the n8n container.
# Uses the Docker service name as hostname (resolved within the shared Docker network).
N8N_BASE_URL=http://n8n:5678

# Port the middleware listens on inside the container.
# Only change this if you also update the Traefik label and Dockerfile EXPOSE.
PORT=3000

# Traefik public domain for middleware proxy
N8N_MIDDLEWARE_HOST=n8n.mydomain.com

# GitHub webhook secret verfifications
GITHUB_WEBHOOK_SECRET=klklsjfklsjfklafaf;l

# Expose the n8n web interface (SPA, static assets, /rest/* internal API) through the middleware.
# Note: WebSocket connections (used for live execution status) are not supported.
# Default: false
N8N_EXPOSE_UI=false

# Expose the n8n public REST API (/api/v1/*) through the middleware.
# Default: false
N8N_EXPOSE_API=false
